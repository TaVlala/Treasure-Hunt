{
  "name": "@treasure-hunt/shared",
  "version": "1.0.0",
  "private": true,
  "description": "Shared TypeScript types, constants, and utilities used across all apps",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "type-check": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "typescript": "^5.7.2"
  }
}
